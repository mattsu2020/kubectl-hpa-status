version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gocritic
    - gocyclo
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused

  settings:
    gocyclo:
      min-complexity: 15
    staticcheck:
      checks:
        - all
        - -QF1012 # WriteString(Sprintf) is acceptable for readability

formatters:
  enable:
    - gofmt
    - goimports
